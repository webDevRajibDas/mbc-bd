@extends('layouts.app')
@section('title', 'Products')
@section('content')
<div class="app-title"><div><h1><i class="fa fa-cube"></i> E-Shop Products</h1></div><ul class="app-breadcrumb breadcrumb"><li class="breadcrumb-item">E-Shop</li><li class="breadcrumb-item active">Products</li></ul></div>
<div class="tile"><div class="tile-title-w-btn"><h3 class="title">Product List</h3><a class="btn btn-primary btn-sm" href="{{ route('admin.e-shop.products.create') }}"><i class="fa fa-plus"></i> Add Product</a></div><div class="tile-body"><div class="table-responsive"><table class="table table-hover table-sm" id="shopProductTable"><thead><tr><th>#</th><th>Product</th><th>Category</th><th>Price</th><th>Stock</th><th>Status</th><th class="text-center">Action</th></tr></thead><tbody>@forelse($products as $product)<tr><td>{{ $loop->iteration }}</td><td>@if($product->image)<img src="{{ asset('storage/'.$product->image) }}" alt="" style="width:38px;height:38px;object-fit:cover" class="mr-2 rounded">@endif<strong>{{ $product->name }}</strong><br><small class="text-muted">{{ $product->sku ?: 'No SKU' }}{{ $product->is_featured ? ' · Featured' : '' }}</small></td><td>{{ $product->category->name ?? 'Uncategorized' }}</td><td>৳ {{ number_format($product->sale_price ?? $product->price, 2) }}@if($product->sale_price)<br><small class="text-muted"><s>৳ {{ number_format($product->price, 2) }}</s></small>@endif</td><td><span class="badge badge-{{ $product->stock_quantity ? 'info' : 'danger' }}">{{ $product->stock_quantity }}</span></td><td><span class="badge badge-{{ $product->is_active ? 'success' : 'secondary' }}">{{ $product->is_active ? 'Active' : 'Inactive' }}</span></td><td class="text-center"><a class="btn btn-success btn-sm" href="{{ route('admin.e-shop.products.edit', $product) }}"><i class="fa fa-pencil"></i> Edit</a> <form class="d-inline" method="POST" action="{{ route('admin.e-shop.products.destroy', $product) }}">@csrf @method('DELETE')<button class="btn btn-danger btn-sm delete-confirm" data-name="{{ $product->name }}"><i class="fa fa-trash"></i> Delete</button></form></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-4">No products found.</td></tr>@endforelse</tbody></table></div></div></div>
@endsection
@push('scripts')<script>$('#shopProductTable').DataTable({responsive:true,autoWidth:false,order:[]});</script>@endpush
